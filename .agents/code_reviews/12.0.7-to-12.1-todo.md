# AuroraClassic 12.0.7 → 12.1 To-do

基準：WoWUI `live` 12.0.7.68453 (`1f2d1789`) → `ptr` 12.1.0.69189 (`b883b4d1`)。

完整證據與理由見 `2026-08-11-full-review-12.1-migration.md`。

## Release blockers

- [ ] `FrameXML/ChatFrame.lua:23-28`：移除已不存在的 `BattleTagInviteFrame` 路徑，確認 theme 後半能完整執行。
- [ ] `FrameXML/Fonts.lua:18-23`：把固定 RaidWarning slots／`RaidBossEmoteFrame` 改為 `RaidWarningFrame.fontStringPool` lifecycle。
- [ ] `AddOns/Blizzard_AchievementUI.lua:38-49`：改用 `HeaderDetails.Filters.FilterDropdown`、`.SearchBox` 與 `SearchBox.SearchPreviewContainer`，保留原生 layout refresh。
- [ ] `AddOns/Blizzard_Delves.lua:29-31`：改走 `CompanionSlots`，納入 Role／Flavor／Combat／Utility 四個 slot。
- [ ] `AddOns/Blizzard_HousingDashboard.lua:7`：把 HouseDropdown 改到 dashboard root，補 Collection／Initiatives。
- [ ] `AddOns/Blizzard_WeeklyRewards.lua:102-104`：處理 `ConcessionsFrame.Rewards.ConcessionFrame1/2`。
- [ ] `AddOns/Blizzard_WeeklyRewards.lua:84-90`：將不存在的 `WeeklyRewardsFrameNameFrame` 改為確認框自身 `ItemFrame.NameFrame`。
- [ ] 全部 blocker 修完後，把 `AuroraClassic.toc` Interface 更新到實際 12.1 MAINLINE 值（預期 `120100`）。

## Secret／secure migration

- [ ] `Blizzard_CooldownViewer.lua:74-87`：重新設計 restricted aura 的 dispel border；Aurora tainted hook 不讀 secret aura payload／RGB，也不能在 skip refresh 後留下 pooled frame 舊色。
- [ ] `Blizzard_Communities.lua:62-73,336`：chat messaging lockdown 不讀／分支 `memberInfo.classID`。
- [ ] `FrameXML/ChatFrame.lua:110-117`：voice GUID 為 secret 時不衍生、測試或索引 class。
- [ ] 實測所有 dynamic `HookScript` 是否通過 12.1 `ScriptBindings`／assignable-script 限制。
- [ ] 審查 `B.HideObject()` 的 `UnregisterAllEvents`＋`SetParent`，以及 protected frame 的 button/statusbar texture replacement、SetPoint/SetAllPoints。
- [ ] 實測 Achievement、AuctionHouse、RaidUI 等 secure addon 在戰鬥中開啟／刷新。

## 12.1 新 UI／視覺覆蓋

- [ ] SocialUI／AddFriend；同時保留 Social UI disabled 的 legacy FriendsFrame 路徑。
- [ ] Housing Collection、Initiatives、HousingBlueprint。
- [ ] CooldownViewer GroupBuffFilter、EditAlert／VisualAlerts、DraggedItem 與 secure lifecycle。
- [ ] PVP `RandomTrainingGroundArenaButton`。
- [ ] Chat Config `ChatConfigOtherSettingsAdditionalColors`。
- [ ] GuildControl Discord linked／unlinked templates。
- [ ] 所有新增 `[Bootstrap]` addon 測 already-loaded 與 delayed ADDON_LOADED 兩條時序。

## 既有缺陷一併清理

- [ ] `AlertFrames.lua:27-43`：統一冪等 marker，停止 pooled frame 重複掛 hook。
- [ ] `Core.lua:162-174`：恢復 `B:CreateSD(size, override)` 契約。
- [ ] `Config.lua:9`／`GUI.lua:114`：讓 ChatBubbles option 真正控制 runtime，或移除無效控制項。
- [ ] `Blizzard_GMChatUI.lua:21-28`：hook canonical `ChatFrameUtil.ActivateChat/DeactivateChat`。
- [ ] PVP `CategoryButton5`、PlayerChoice `BorderOverlay`、NewPlayer 現行 globals、Transmog `PreviewedWeaponToggle`。
- [ ] `InitiativeTasksObjectiveTracker` 納入 ObjectiveTracker 通用 lifecycle。
- [ ] ExpansionLandingPage 改為 Midnight overlay；MajorFactionRenown 責任轉到 EncounterJournal Journeys。
- [ ] 清理 MAINLINE dead themes：DelvesDashboardUI、TalentUI、Tutorial、VoidStorageUI。
- [ ] `Init.lua:67-70` 改成 `C.themes[addon] = nil`。
- [ ] 移除 Calendar debug print 與未使用 `DB.isNewPatch`。
- [ ] 將 `B.ReplaceIconString`、`B.SetCurrenciesHook` 等真正共用 helper 移到 Core，或 caller local 化。

## 驗證清單

- [ ] 冷登入（關閉 Load out of date AddOns）與 `/reload`。
- [ ] 每個 LoD addon：Aurora 先載入後首次開 UI；Blizzard addon 已載入後才載入／重載 Aurora。
- [ ] ChatFrame 後半、`/rw`、boss emote、Achievement search/comparison、Brann 四 slot、Housing 三頁、Great Vault 兩 concession／選獎。
- [ ] ScrollBox／pool：捲出入、ReleaseAll／reacquire、搜尋／篩選、資料與分頁切換。
- [ ] 戰鬥中開啟／刷新、離戰恢復、secure drag／menu。
- [ ] restricted target aura、chat messaging lockdown、voice notification。
- [ ] SocialUI enabled/disabled、PVP Training Ground Arena、Housing Blueprint、Guild Discord tab、CooldownViewer 新 alert/filter。
- [ ] GUI：ChatBubbles=false、Shadow=false＋VenturePlan、FontScale 多次 reload 不累乘。
- [ ] `git diff --check`、完整 diff、TOC/XML/theme key 與改名符號殘留搜尋。

## 完成條件

- 所有 release blocker 已修、沒有已知 nil-global／nil-field error。
- 12.1 Interface 已更新，冷登入不依賴「載入過期插件」。
- 所有 secret／secure 高風險場景已在正式服驗證，沒有 Lua error、blocked action 或 taint。
- 所有 pool／ScrollBox 測試沒有重複 backdrop/hook、舊狀態殘留或未 skin 的 reused frame。
- README／release notes 清楚標示 12.1 支援與尚未支援的選配第三方整合。
