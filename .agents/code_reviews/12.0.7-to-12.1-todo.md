# AuroraClassic 12.0.7 → 12.1 To-do

正式基準：WoWUI `live` 12.0.7.68974 (`861fbf13`) → `live` 12.1.0.69273 (`b3733541`, tag `v12.1.0`)。

2026-08-12 已用正式 live 重核：原六個 release blocker 全部成立，並已在當前工作樹完成靜態修正；先前 PTR 69189 只保留為歷史來源。最後 PTR → live 沒有新增第七個現有 runtime blocker。正式服驗證仍列在下方清單。

完整證據與理由見 `2026-08-11-full-review-12.1-migration.md`。

## Release blockers

- [x] `FrameXML/ChatFrame.lua`：移除已不存在的 `BattleTagInviteFrame`，改用 `BattleNetInviteFrame` 具名 children。
- [x] `FrameXML/Fonts.lua`：把固定 RaidWarning slots／`RaidBossEmoteFrame` 改為 `fontStringPool`／`AcquireString` lifecycle。
- [x] `AddOns/Blizzard_AchievementUI.lua`：改用 `HeaderDetails.Filters.FilterDropdown`、`.SearchBox` 與 `SearchBox.SearchPreviewContainer`，保留原生 layout refresh。
- [x] `AddOns/Blizzard_Delves.lua`：改走 `CompanionSlots`，納入 Role／Flavor／Combat／Utility 四個 slot；live 確認四者都有 `OptionsList.ScrollBox`。
- [x] `AddOns/Blizzard_HousingDashboard.lua`：改用 dashboard root 容器內真正的 `HouseDropdown.Dropdown` widget。
- [x] `AddOns/Blizzard_WeeklyRewards.lua`：枚舉 `ConcessionsFrame.Rewards:GetChildren()`，處理每個 concession 的 `RewardsFrame.Text`，不把數量 2 寫死。
- [x] `AddOns/Blizzard_WeeklyRewards.lua`：將不存在的 `WeeklyRewardsFrameNameFrame` 改為確認框自身 `ItemFrame.NameFrame`。
- [x] 依使用者指示把 `AuroraClassic.toc` Interface 更新到 12.1 目標值 `120100`；runtime Interface 與冷登入仍列在驗證清單。

## Secret／secure migration

- [x] `Blizzard_CooldownViewer.lua`：已移除 restricted aura payload／RGB consumer 與自訂 per-aura border state，保留 Blizzard 原生 DebuffBorder；此 secret 契約在 12.0.7 已存在，12.1 另新增 `RequiresUnitAuraAccess`。
- [x] `Blizzard_Communities.lua`：roster 已移除 `memberInfo.classID` consumer，改用原生 Class widget 並 guard shown secret aspect；屬 12.0.7 已存在的風險。
- [x] `FrameXML/ChatFrame.lua`：voice GUID 為 secret／chat lockdown 時不衍生、測試或索引 class，保留原生 channel color。
- [ ] 新增 SocialUI skin 時不 hook、解析或重送 `C_BattleNet.SearchFriends` 的 `AuroraFriendsSearchInfo`；live API 已標 `HasRestrictions=true`、`SecretArguments=AllowedWhenUntainted`。
- [ ] 新增 HousingBlueprint skin 時不讀、trim、驗證或保存 share code；`C_HousingBlueprint.UpdateBlueprintStringFromInput` 的 secret argument 只允許未 taint 呼叫。
- [ ] 實測所有 dynamic `HookScript` 是否通過 12.1 `ScriptBindings`／assignable-script 限制。
- [ ] 審查 `B.HideObject()` 的 `UnregisterAllEvents`＋`SetParent`，以及 protected frame 的 button/statusbar texture replacement、SetPoint/SetAllPoints。
- [ ] 實測 Achievement、AuctionHouse、RaidUI 等 secure addon 在戰鬥中開啟／刷新。

## 12.1 新 UI／視覺覆蓋

- [x] 新增 `BattleNetInviteFrame` skin。
- [ ] 既有 `AddFriendFrame` skin 做回歸驗證。SocialUI 另做 pool-aware skin，同時保留 Social UI disabled 的 legacy FriendsFrame 路徑；不覆寫 live 的 `OnTextChanged`、`OnHide`、`InitializeFilterBar` 或 `GenerateFilterMenu`。
- [ ] Housing Collection、Initiatives；新增獨立 `Blizzard_HousingBlueprint` LoD theme/module 與 AddOns XML 項目，不把 Blueprint skin 綁在 Dashboard theme。
- [ ] HousingBlueprint import input／validation 兩個 `GearDropdown`，以及 ContentSummary 的 loading／error／empty/content visibility、`fixedHeight`／`minimumHeight` 與 Layout/MarkDirty lifecycle；`ContentBudgets` pool 冪等 skin，且不可 force-show children。
- [ ] CooldownViewer GroupBuffFilter、EditAlert／VisualAlerts、DraggedItem 與 secure lifecycle。
- [ ] Communities 新 Discord stream／`discordInfo`／friend-request surface；只依 frame/region state 套外觀，不解析 member payload。
- [ ] PVP 枚舉原生 `BonusTrainingGroundButtons` parent array，涵蓋 `RandomTrainingGroundButton` 與 `RandomTrainingGroundArenaButton`。
- [ ] Chat Config `ChatConfigOtherSettingsAdditionalColors`。
- [ ] GuildControl Discord linked／unlinked templates。
- [ ] 所有新增 `[Bootstrap]` addon 測 already-loaded 與 delayed `ADDON_LOADED` 兩條時序；initial scan 已由 live Blizzard `EventUtil.ContinueOnAddOnLoaded` 契約確認雙回傳判斷正確。

## Live source 已排除／上游異常

- [x] `1f2d1789`（12.0.7.68453）→ `861fbf13`（12.0.7.68974）只有 Aurora 未引用的 HouseEditor saved-state key 變更，不新增 migration 工作。
- [x] 12.1 QueueStatus 新增 `LE_LFG_CATEGORY_LAIR`，但 Aurora 只處理 `QueueStatusFrame` 外框、不讀 category，無需改碼。
- [ ] 上游觀察：live `Blizzard_HousingDashboardInitiatives.lua:177` 仍以已移除的 `HouseInfoContent.HouseDropdown` 顯示 HelpTip。Aurora 不修 Blizzard source；正式服測 Initiatives 首次顯示時分辨原生與 Aurora 錯誤。

## 既有缺陷一併清理

- [x] `AlertFrames.lua:27-43`：已改用一致的專用冪等 marker，停止 pooled frame 重複掛 hook。
- [x] `Core.lua:162-174`：已恢復 `B:CreateSD(size, override)`、既有 shadow 回傳與 override 契約。
- [x] `Config.lua:9`／`GUI.lua:114`：ChatBubbles option 已在 default theme 入口控制 runtime；採 reload-based 行為。
- [x] `Blizzard_GMChatUI.lua:21-28`：已 hook canonical `ChatFrameUtil.ActivateChat/DeactivateChat`，並以 `editBox == GMChatFrameEditBox` 精確過濾。
- [x] PVP category 改枚舉 live `CategoryButtons`（包含 `CategoryButton5`）；PlayerChoice 補 `BorderOverlay`、NewPlayer 改現行 globals、Transmog 補 `PreviewedWeaponToggle`。
- [x] `InitiativeTasksObjectiveTracker` 納入 ObjectiveTracker header／block／progress／timer 通用 lifecycle。
- [x] ExpansionLandingPage 改為動態 Midnight `overlayFrame`；EncounterJournal Journeys 補固定 BorderFrame 與 pooled Renown `WatchFactionCheckbox`。
- [x] 移除只指向 dead `MajorFactionRenownFrame` 的 Aurora module／XML 項目；`Blizzard_MajorFactions` addon key 本身仍有效，現行外觀責任由 EncounterJournal Journeys 承接。
- [x] 清理 MAINLINE dead themes：DelvesDashboardUI、TalentUI、VoidStorageUI；Talent／Void Lua 與 XML Script 已同步刪除，Delves 保留其他有效 themes。
- [ ] 決定 `Blizzard_Tutorial` 是否依 live `Blizzard_BoostTutorial`／PortraitFrameTemplate 重寫；不能只 re-key。
- [x] `Init.lua:67-70` 已改成 `C.themes[addon] = nil`。
- [x] 移除 Calendar debug print 與未使用的 `DB.isNewPatch`。
- [ ] 將 `B.ReplaceIconString`、`B.SetCurrenciesHook` 等真正共用 helper 移到 Core，或 caller local 化；現況是模組所有權／刪檔耦合，不是 theme 執行前 helper 尚未定義。

## 驗證清單

- [ ] 正式服 `/dump select(4, GetBuildInfo())` 回傳 `120100`。
- [ ] 冷登入（關閉 Load out of date AddOns）與 `/reload`。
- [ ] 每個 LoD addon：Aurora 先載入後首次開 UI；Blizzard addon 已載入後才載入／重載 Aurora。
- [ ] ChatFrame 後半、`/rw`、boss emote、Achievement search/comparison、Brann 四 slot、Housing 三頁、Great Vault 兩 concession／選獎。
- [ ] 既有外觀漏項：PVP 第五 category、PlayerChoice 新／舊 texture kit、新手教學移動鍵與 single-key frame、Transmog Previewed Weapon、Initiative Tasks tracker、Midnight overlay，以及 Journeys Renown card 捲動／hover／pool reuse。
- [ ] ScrollBox／pool：捲出入、ReleaseAll／reacquire、搜尋／篩選、資料與分頁切換。
- [ ] 戰鬥中開啟／刷新、離戰恢復、secure drag／menu。
- [ ] restricted target aura、chat messaging lockdown、voice notification。
- [ ] SocialUI enabled/disabled、逐字搜尋/filter/BN disconnect/close-reopen、PVP Training Ground Arena、Housing Blueprint 全狀態、Guild Discord tab、CooldownViewer 新 alert/filter。
- [ ] GUI：ChatBubbles=false、Shadow=false＋VenturePlan、FontScale 多次 reload 不累乘。
- [ ] `git diff --check`、完整 diff、TOC/XML/theme key 與改名符號殘留搜尋。

## 完成條件

- 所有 release blocker 已修、沒有已知 nil-global／nil-field error。
- 12.1 Interface 已更新，冷登入不依賴「載入過期插件」。
- 所有 secret／secure 高風險場景已在正式服驗證，沒有 Lua error、blocked action 或 taint。
- 所有 pool／ScrollBox 測試沒有重複 backdrop/hook、舊狀態殘留或未 skin 的 reused frame。
- README／release notes 清楚標示 12.1 支援與尚未支援的選配第三方整合。
